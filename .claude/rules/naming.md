---
paths: ["**/*"]
alwaysApply: true
description: "命名慣例"
---

# 命名慣例

- Class / Service：`PascalCase.ts`（例如 `PositionScanner.ts`）
- 純函式模組 / Runner：`camelCase.ts`（例如 `marketData.ts`）
- 常數：`UPPER_SNAKE_CASE`
- TypeScript：strict mode，**禁止 `any`**